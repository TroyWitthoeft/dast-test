name: ZAP-Scan
run-name: Let's Go 🚀
on: [push]
jobs:
  Zap-Scan:
    runs-on: ubuntu-latest
    name: Scan the webapplication
    steps:
      - name: ZAP-Scan
        uses: zaproxy/action-baseline@v0.7.0
        with:
          target: 'https://glsauto.com'
